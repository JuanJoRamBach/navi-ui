// Single source of truth for NAVI's backend origin — used by both the
// push-subscribe flow (push.ts) and the real chat endpoint (App.tsx).
export const NAVI_BACKEND_URL = "https://navi-fih8.onrender.com";
